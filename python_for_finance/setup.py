from setuptools import find_packages, setup

setup(name='python_for_finance',
      version='0.0.1-dev',
      description='My app package',
      install_requires=['tkinter'],
      packages=find_packages(),
      zip_safe=False)
